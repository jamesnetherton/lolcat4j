name: Lolcat4j Release

on:
  push:
    tags:
      - '*'

jobs:
  build-jar:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get-version.outputs.version }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6
      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: temurin
      - name: Cache Maven Artifacts
        uses: actions/cache@v5
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2
      - name: Build JAR
        run: mvn -B -ntp package -DskipTests
      - name: Get Version
        id: get-version
        run: echo "version=${{ github.ref_name }}" >> $GITHUB_OUTPUT
      - name: Upload JAR
        uses: actions/upload-artifact@v4
        with:
          name: jar
          path: target/lolcat4j-${{ github.ref_name }}.jar

  build-native:
    needs: build-jar
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux-amd64
            ext: ''
          - os: windows-latest
            platform: windows-amd64
            ext: .exe
          - os: macos-latest
            platform: macos-aarch64
            ext: ''
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6
      - name: Set up GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          java-version: '21'
          distribution: graalvm
      - name: Cache Maven Artifacts
        uses: actions/cache@v5
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2
      - name: Build JAR
        run: mvn -B -ntp package -DskipTests
      - name: Build Native Image (Unix)
        if: runner.os != 'Windows'
        run: |
          native-image -jar target/lolcat4j-${{ github.ref_name }}.jar \
            -H:Name=lolcat4j-${{ github.ref_name }}-${{ matrix.platform }} \
            --no-fallback
      - name: Build Native Image (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: native-image -jar target/lolcat4j-${{ github.ref_name }}.jar -H:Name=lolcat4j-${{ github.ref_name }}-${{ matrix.platform }} --no-fallback
      - name: Upload Native Binary
        uses: actions/upload-artifact@v4
        with:
          name: lolcat4j-${{ github.ref_name }}-${{ matrix.platform }}${{ matrix.ext }}
          path: lolcat4j-${{ github.ref_name }}-${{ matrix.platform }}${{ matrix.ext }}

  release:
    needs: [build-jar, build-native]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download All Artifacts
        uses: actions/download-artifact@v8
        with:
          merge-multiple: true
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ github.ref_name }}
          files: |
            lolcat4j-${{ needs.build-jar.outputs.version }}.jar
            lolcat4j-${{ needs.build-jar.outputs.version }}-linux-amd64
            lolcat4j-${{ needs.build-jar.outputs.version }}-windows-amd64.exe
            lolcat4j-${{ needs.build-jar.outputs.version }}-macos-aarch64
